Drop .wav (or .ogg / .mp3) loop files in this folder and they will be auto-discovered
by AudioManager via Resources.LoadAll<AudioClip>("AudioLoop").

Each found clip will appear in the Settings menu's Music Track selector.
A built-in "Procedural" track is always available as a fallback even if this folder is empty.
